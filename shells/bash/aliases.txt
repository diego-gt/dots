# utilities
alias ls='ls --color=auto'
alias ll='ls --color=auto --almost-all --human-readable -l'

alias gs='git status'
alias ga='git add'
alias gc='git commit'
alias gd='git diff'
alias gdh='git diff HEAD'
alias gf='git fetch'
alias gpl='git pull'
alias gps='git push'

# editors
alias v='nvim'
alias vrc='nvim $HOME/.config/nvim/init.lua'

# tmux
alias ts='tmux new-session -s $(printf '%s' "${PWD##*/}")'

# directories
alias dots='cd $GIT_REPOS/dots'
alias ws='cd $HOME/dev'
alias gitp='cd $GIT_REPOS'

